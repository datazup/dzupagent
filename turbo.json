{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["YARN_CACHE_FOLDER", "YARN_GLOBAL_FOLDER", "npm_config_prefix"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@dzupagent/memory#build": {
      "dependsOn": ["@dzupagent/memory-ipc#build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build", "^typecheck"],
      "outputs": []
    },
    "@dzupagent/memory#typecheck": {
      "dependsOn": ["@dzupagent/memory-ipc#build"]
    },
    "lint": {
      "inputs": ["src/**", "package.json", ".eslintrc*", "eslint.config.*"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "test/**", "package.json", "tsconfig.json", "vitest.config.*"],
      "outputs": []
    },
    "@dzupagent/evals#test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "tsconfig.json", "vitest.config.*"],
      "outputs": []
    },
    "@dzupagent/rag#test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "tsconfig.json", "vitest.config.*"],
      "outputs": []
    },
    "@dzupagent/test-utils#test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "tsconfig.json", "vitest.config.*"],
      "outputs": []
    },
    "@dzupagent/testing#test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "tsconfig.json", "vitest.config.*"],
      "outputs": []
    },
    "@dzupagent/server#test": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "tsconfig.json", "vitest.config.*"],
      "outputs": []
    }
  }
}
