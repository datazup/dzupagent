/**
 * ECO-179: Benchmark Suite Types
 *
 * As of MC-A02, the shared types (BenchmarkCategory, BenchmarkSuite,
 * BenchmarkResult, BenchmarkComparison) are canonically defined in
 * @dzupagent/eval-contracts and re-exported here for backward compatibility.
 */

export type {
  BenchmarkCategory,
  BenchmarkSuite,
  BenchmarkResult,
  BenchmarkComparison,
} from '@dzupagent/eval-contracts';
