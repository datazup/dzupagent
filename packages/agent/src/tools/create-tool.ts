/**
 * Re-export of `createForgeTool` from `@dzupagent/core`.
 *
 * @deprecated Import from `@dzupagent/core` directly. This re-export exists
 * for backward compatibility only.
 */
export { createForgeTool } from '@dzupagent/core'
export type { ForgeToolConfig } from '@dzupagent/core'
