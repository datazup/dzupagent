/**
 * @dzupagent/agent-adapters/enrichment — context enrichment pipeline.
 *
 * Use this subpath to invoke the EnrichmentPipeline without depending on
 * the full agent-adapters root surface.
 */

export { EnrichmentPipeline } from './enrichment/enrichment-pipeline.js'
