/**
 * X4 Fleet qualification receipts — thin barrel.
 *
 * The implementation was decomposed into focused leaf modules under
 * `./fleet-qualification/`. This file preserves the original public import
 * path (`./fleet-qualification.js`) so existing consumers are unaffected.
 */
export * from "./fleet-qualification/index.js";
