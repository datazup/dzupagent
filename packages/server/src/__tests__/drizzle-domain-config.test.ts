// MOVED to @ai-saas/server-domain — see:
// apps/ai-saas-starter-kit/packages/server-domain/src/__tests__/drizzle-domain-config.test.ts
import { describe, it } from 'vitest'

describe.skip('createDrizzleDomainConfig (moved to @ai-saas/server-domain)', () => {
  it('tests live in the consuming app', () => { /* placeholder */ })
})
