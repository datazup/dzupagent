# DzupAgent Architecture Documentation — moved

The comprehensive architecture documentation set and the distributed-runtime
implementation plans now live in the durable docs repository:

**→ `workspace-docs/repos/dzupagent/docs/architecture/`**

Contents:

- `01-overview.md` … `09-distributed-platform-roadmap.md` — the architecture guide
- `plans/` — Phase-1 (and beyond) implementation plans (P0–P4)
- `UNIFIED_ROADMAP.md` — the merged implementation program
- (alongside) `../specs/process-crash-safe-dsl-orchestration-2026-06-16.md` — the upstream crash-safe DSL spec

Durable architecture/planning docs are kept in `workspace-docs/` per the
workspace convention; this stub is the repo-local pointer.
