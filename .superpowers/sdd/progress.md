# SDD Progress: Runtime-Determinism Primitives (2026-07-03)

Plan: workspace-docs/repos/dzupagent/docs/plans/2026-07-03-runtime-determinism-primitives-implementation-plan.md
Branch: feat/runtime-determinism-primitives, base main@7a038abf
Worktree: /media/ninel/Third/code/datazup/ai-internal-dev/.worktrees-dzupagent/runtime-determinism-primitives

## Tasks (8 total, 3 phases)

- Task 1 (persistence barrel merge): pending
- Task 2 (RunStateApi): pending
- Task 3 (InterruptOutcome contract, adapter-types): pending
- Task 4 (hitl-kit ApprovalOutcome alias): pending
- Task 5 (SpawnGate migration): pending
- Task 6 (RunJournal flushPolicy + durability doc): pending
- Task 7 (ReplayController fork): pending
- Task 8 (whole-workspace verification): pending

## Log

Task 1: complete (commit 7a038abf..1d760ded, review clean — spec ✅, quality Approved). Barrel merge: persistence/index.ts now canonical superset (added InMemoryRunStore/InMemoryAgentStore, InMemoryEventLog/EventLogSink, InMemoryRunStateStore, InMemoryRunRecordStore, DeltaRunStateStore + types); persistence.ts reduced to single re-export line; core/src/index.ts untouched (confirmed). 2/2 barrel test, 4715/4715 full core suite, 58/58 dependent-package typecheck. Reviewer noted `versioned-context-backend.ts` remains unexposed by either barrel — OUT OF SCOPE by design (not named in brief/spec, is a separate "designed-but-not-adopted" module per original exploration) — not a gap, no follow-up needed.
Task 2: complete (commit 20536baa..dd643904, review clean — spec ✅, quality Approved). RunStateApi (getState/getStateHistory) over DzupRunStateStore+RunJournal in new run-state-api.ts. Implementer corrected 2 real bugs in the plan's sketched code (RunJournalQuery uses afterSeq not fromSeq/toSeq; resumeToken belongs to RunResumedEntry not RunPausedEntry) — both independently re-verified by reviewer against live run-journal-types.ts, confirmed accurate. summarizeEntry exhaustive over all 13 real entry types w/ never-check. 3/3 new tests, 4718/4718 full core suite.
Task 3: complete (commit dd643904..81cd0819, review clean — spec ✅, quality Approved). InterruptOutcome/InterruptGate/deriveInterruptId in new packages/adapter-types/src/contracts/interrupt.ts, barrel-exported. 2/2 new tests, 56/56 full package suite. MINOR(carry to final): test file placed at src/contracts/__tests__/interrupt.test.ts per brief's literal instruction, but package convention is flat src/__tests__/ importing from barrel — cosmetic, no functional impact, candidate normalize-later.
Task 4: complete (commit 16be83ac..9ce461b9, review clean — spec ✅, quality Approved). ApprovalOutcome aliased to shared InterruptOutcome. REAL BLOCKER hit + resolved: discriminated union broke 31 un-narrowed test assertions in hitl-approval-gate-deep.test.ts (runtime expect().toBe() isn't a TS type guard) — controller-authorized fix: add real if/throw narrowing before .response/.reason access at all 31 sites, zero assertion values changed, verified by reviewer against real InterruptOutcome union. package.json dependency pinned "0.2.0" matching subagents' convention (brief said "*", correctly deviated). 248/248 tests, typecheck 0 errors, 53/53 blast-radius tasks.
Task 5: complete (commit 9ce461b9..b975129c, review clean — spec ✅, quality Approved). SpawnApprovalGate migrated to waitForInterrupt(...): Promise<InterruptOutcome>. Implementer proactively found+fixed a real production call site beyond brief scope (background-subagent-runtime.ts's resolveApprovalThenAdmit consumed old .approved/.reason shape) using Task-4-established narrowing pattern; also updated 3 test fakes + removed dead ApprovalOutcome export from index.ts (confirmed genuinely dead via workspace grep) + added OWN-WRAP-CONVERGE.md trigger bullet. 53/53 subagents tests, agent-adapters (only downstream consumer) typechecks clean, live re-verified by reviewer.
Task 6: complete (commit b975129c..935f729c, review clean — spec ✅, quality Approved w/ 2 Minors carried). flushPolicy?: 'sync'|'async' added to RunJournal.append() as declared-but-no-op (NO hook mechanism exists anywhere in codebase — grepped/confirmed independently by reviewer, InMemoryRunJournal is sole RunJournal impl). Module-doc paragraph in run-journal.ts correctly documents durability-contract independence from FlowDocumentV1.durability.mode/DurableNodeLedger (ADR Decision 3). 3/3 new tests non-vacuous, 4721/4721 full core suite, typecheck clean, all real append() call sites verified 2-arg (non-breaking). MINOR(carry to final): no-op-ness documented at interface-level JSDoc (good) but concrete InMemoryRunJournal impl only has terser inline comment — suggest future dev-mode warn if flushPolicy set with no hook registered. MINOR(carry to final): diff includes unrelated quote/semicolon formatter reflow across run-journal-types.ts/in-memory-run-journal.ts, cosmetic only, inflates diff size.
Task 7: complete (commit 935f729c..4ebe9167, review clean — spec ✅ justified adaptation, quality Approved). SIGNIFICANT DESIGN DEVIATION, verified sound: plan assumed ReplayController(engine) + sessionId-keyed methods; REAL API is ReplayController(session) bound to ONE session at construction, zero sessionId-keyed methods anywhere (confirmed across 55 existing call sites, not just ~30 estimated). Implementer adapted: fork(atIndex) on bound session (no sessionId param — nothing to look up), optional 2nd constructor param engine? for registration, ReplaySessionNotFoundError repurposed to fire on engine-missing/session-deleted-from-registry. Fully backward compatible (0/55 existing call sites pass 2nd arg). generateSessionId exported+reused (not duplicated) for consistent ID scheme. Reviewer independently verified mutation-isolation + breakpoint-isolation tests are genuinely non-vacuous (would fail on shared references). 182/182 replay suite, typecheck clean, lint skipped (reproduced: real eslint-not-on-PATH environment gap, not task-introduced).

## ALL 7 IMPLEMENTATION TASKS COMPLETE. Task 8 (whole-workspace verification) next.
Task 8: complete (commits 79bc62ad..4392bc21, review clean — spec ✅, quality Approved). Whole-workspace verification: typecheck 62/62, verify gates all pass except 3 independently-proven pre-existing/unrelated (memory dist missing in fresh worktree, server-api-surface doc staleness, token-bucket timing flake under load). 2 real governance fixes made+reviewer-reproduced: hitl-kit reclassified layer0->layer1 (now has real adapter-types dep, was violating same-layer-edge rule; verified consumers all layer>=2, zero new violations), barrel-budget re-pin +1 export each for core (RunJournalAppendOptions)/adapter-types (interrupt.js barrel line) - confirmed exact +1 delta, not inflation. yarn.lock sync fix confirmed minimal (1 line). Gap-review doc updated+committed in workspace-docs repo (separate git repo per workspace structure) marking all 4 items shipped w/ commit refs.

## ALL 8 TASKS COMPLETE. Branch feat/runtime-determinism-primitives ready for final whole-branch review.
Final HEAD: 4392bc21 (dzupagent worktree). Base: 7a038abf (main).
