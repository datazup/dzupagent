# SDD Progress: MPCO Runner P8 (executable review-loop) — PLANNING (started 2026-06-29)

NEXT SLICE PLAN being authored (opus subagent, source-verified): workspace-docs/repos/scripts/docs/mpco-runner-p8-plan-2026-06-29.md — the keystone executable review-loop runner (deps P5+P6/P7+P8a all DONE). Wires bridge selection (interactive→Runtime/operator→Operator) + governance ports (real createBudgetGovernor→checkBudget/accrueUsage + markBudgetExceeded on terminal-over-budget — executor deliberately doesn't) + cross_validate cross-family enforcement + order randomization + confidence-aware critique gate + bounded-revise subgraph (T13) into the Option-B canonical-AST executor. Keystone gate = e2e compile-AND-execute fake adapter.run+validate flow. VERIFIED branch base: dzupagent feat/mpco-governance (308ed3f3) STACKS COMPLETE MPCO dzupagent stack P2(6f3d6b73)/P3(33dab0ab)/P4(68284ddc)/P6(041c2565+b5716afa)/P8a(0920297b+308ed3f3) — correct base if P8 needs dzupagent change. scripts continues feat/mpco-approval-bridges (executor+bridges+governor all here). NOTE: first plan-author dispatch hit a connection error mid-run after 22 verification reads; resumed same agent (a18976419e07a592d) to write the plan from its retained context. AWAITING plan file + my review.

---

# SDD Progress: MPCO Governance P8a (T15 budget + T16 circuit) — COMPLETE (2026-06-29) — see READY-TO-MERGE=YES block below

Plan: workspace-docs/repos/scripts/docs/mpco-governance-p8a-plan-2026-06-29.md (opus-authored, source-verified, NO blockers — circuit breaker AdapterHealthMonitor.canExecute @health-monitor.ts:86 live; executor pre-call seam execute-canonical-ast.mjs:26 ports.runAdapter live; both self-reviewed vs source).
dzupagent branch: feat/mpco-governance base b5716afa (cut from feat/mpco-approval-bridges tip = PR#14, carries P1-P6). adapter-types typecheck BASELINE = 0 (clean) → Tasks1/2 gate is literal EXIT 0.
scripts branch: feat/mpco-approval-bridges (CONTINUE — only branch with both executor P1b + bridges P6/P7; tip moving via daemon). Commit by EXPLICIT PATH only (daemon active).

## FINAL WHOLE-BRANCH REVIEW (opus, both repos): READY-TO-MERGE = YES. 0 Crit / 0 Imp. All 6 cross-cutting invariants VERIFIED IN SOURCE: (1) TS↔JS accumulation parity FIELD-FOR-FIELD + boundary-for-boundary identical (dzupagent accrueUsage budget.ts:202 vs scripts governor budget-governor.mjs:43 — same 5 fields, same `>` strict breach, both skip undefined caps) — the highest-risk seam, clean; (2) reason literals consistent cross-repo (executor pushes "circuit_open"=circuit-gate.ts literal, "budget_exceeded"=CollabState terminal); (3) budget_exceeded is CollabState run-state NOT CollabGateDecision — disjoint from P6/P7 bridge vocab (writes state/terminationReason vs gateDecision); (4) backward-compat airtight (3 ports truthy-guarded, byte-identical when absent, gateImpact/worstImpact UNTOUCHED); (5) fail-closed (both blocks return before runAdapter, spy adapterCalls===0); (6) purity (dzupagent pure, no clock/IO/mutation) + homing (governor imports only ./collab-persistence.mjs, zero @dzupagent). Executor does NOT call markBudgetExceeded = CORRECT deferred-P8 layering (pure in-memory walker; persistence is runner's job), NOT a gap. ALL Minors DEFER: Task1-M1/M2 cosmetic/adequate; Task1-M3 at-boundary `>` test = cheapest high-value guard, ADD at P8 wiring (both sides simultaneously); Task4-M1 breach-field additive/keep; new M-A dzupagent accrueUsage lacks ?? 0 on required fields (NaN if malformed runtime usage — but scripts side is defensive, harmless today); M-B budget_exceeded event has no ts (verify vs convention at P8). GATES: adapter-types 50/50 ✓ + typecheck EXIT 0 ✓; scripts MPCO 38/38 ✓ (5 governor + 4 executor-gov + 10 pre-existing executor green); root typecheck EXIT=1 but ALL pre-existing/unrelated (arabic authenticate.test NextFunction + template-app dist-race, same as P6/P7 slice; ZERO MPCO-surface errors; adapter-types EXIT 0).

## Slice Status: ALL 4 P8a TASKS COMPLETE + REVIEWED CLEAN + FINAL REVIEW READY-TO-MERGE=YES.

- dzupagent feat/mpco-governance: b5716afa..308ed3f3 (Task1 0920297b + Task2 308ed3f3). NOT merged. Cut from feat/mpco-approval-bridges (PR#14).
- scripts feat/mpco-approval-bridges: Task3 8619fda7 + fix 3ed30880 + Task4 6a358a89 (daemon commits interleaved, zero MPCO files). NOT merged.

## Slice Tasks (P8a)

- Task 1 (T15 types — adapter-types budget.ts): COMPLETE (commit 0920297b, review clean — spec ✅, Approved 0Crit/0Imp). 7/7 tests, typecheck EXIT 0. Purity verified (accrueUsage returns fresh literal, no mutation/clock/IO; NaN-safe via ?? 0 on optional TokenUsage fields; emptyTally fresh each call). CollabState +budget_exceeded purely additive (terminal, last member, nothing reordered). breach semantics `>` strict. MINOR(carry to final): redundant `const u=usage` alias; T15b cost-breach only spot-checks breachedLimit not full shape; no at-boundary (totalTokens===maxTokens → exceeded:false) test for the `>` decision.
- Task 2 (T16 types — adapter-types circuit-gate.ts): COMPLETE (commit 308ed3f3, review clean — spec ✅, Approved 0Crit/0Imp/0Minor verbatim-clean). 3/3 tests, typecheck EXIT 0. PURE — imports ONLY `type AdapterProviderId` from sibling, ZERO agent-adapters/core runtime dep. Decision non-inverted: canExecute=false→{allowed:false,reason:"circuit_open"}, true→{allowed:true}. **LOAD-BEARING for Task 4: blocked reason literal = exactly "circuit_open" (typed literal reason?:"circuit_open").** CircuitGateResult carries provider.
- Task 3 (T15 runtime — scripts/mpco budget-governor.mjs): COMPLETE (impl 8619fda7, review spec ✅ Approved 0Crit/0Imp + 2 Minors → fix dispatched). 5/5 tests. HOMING CLEAN (zero @dzupagent imports, only ./collab-persistence.mjs). Accumulation parity w/ Task1 EXACT (5 fields ?? 0, `>` strict breach). markBudgetExceeded uses ONLY writeCollabTaskSnapshot+appendCollabEvent (state:'budget_exceeded', breach in terminationReason). Closure encapsulated (tally() returns spread copy). Real fs round-trip tests (mkdtemp + read back + ndjson). Exactly 2 files. FIXES DONE (commit 3ed30880, 1 file 2 lines): stale updatedAt→always-fresh; breach wording now "actual N > limit M". 5/5 still green, daemon-clean. Re-review PASS (changes exactly as specified, no regression). Task 3 COMPLETE.
- Task 4 (T15+T16 runtime — scripts/mpco execute-canonical-ast.mjs pre-call gate): COMPLETE (commit 6a358a89, review clean — spec ✅, Approved 0Crit/0Imp). 14/14 tests (10 pre-existing executor tests GREEN unchanged + 4 new T15/T16). Backward-compat AIRTIGHT: all 3 ports optional-guarded (canRunProvider && / checkBudget && / accrueUsage?.), absent-port path byte-equivalent to original. Gate order circuit→budget→run. Fail-closed via early return (runAdapter unreachable on block — spy proves adapterCalls.length===0 on block, ===1 on first-ran-second-blocked). Exact literals reason:"circuit_open"/"budget_exceeded". state.budgetExceeded set on budget block only; gateImpact/worstImpact UNTOUCHED (P4 orthogonal). accrueUsage only after success. Exactly 2 files, no @dzupagent import. MINOR(carry to final): extra `breach` field on budget-blocked step (additive/harmless/useful, not in formal port contract yet).

---

# SDD Progress: MPCO Approval Bridges (P6 → P7) — COMPLETE (2026-06-29)

Plan: workspace-docs/repos/scripts/docs/mpco-approval-bridges-plan-2026-06-29.md
dzupagent branch: feat/mpco-approval-bridges base d9ba56dd (cut from feat/mpco-resolver-envelope-gate tip — carries P2/P3/P4)
scripts branch: feat/mpco-approval-bridges base 9d064b6c (NEW branch cut from scripts main — feat/mpco-foundation was ALREADY MERGED to scripts main d6cca082 by operator/daemon, so all P2-P4 scripts work is in main; commit by explicit path only — autocommitter daemon ACTIVE, has interleaved 2 unrelated commits 9d064b6c+80cd5bb7 on this branch, both touch ZERO mpco files).

## FINAL WHOLE-BRANCH REVIEW (opus, both repos): READY-TO-MERGE = With fixes (only gate = root typecheck green). 0 Critical / 0 Important. All 6 cross-cutting invariants VERIFIED IN SOURCE (not just claimed): (1) shared CollabGateDecision vocab, separate persistence — every emitted value confirmed a real union member vs collab-task.ts:20-27, bridges never read each other's state; (2) two-phase HMAC load-bearing — traced verifyApprovalHmac (ledgerPath defaults null, appendLedger gated behind `if(ledgerPath && approval.iat)`) so verify provably never writes; consumeReplayTokenOnce sole+exactly-once writer; resume prefers snapshot → no re-consume; (3) ensurePending catches DuplicateApprovalError SPECIFICALLY + rethrows others (works w/o Task1 patch); (4) createPending patch LOOSENS contract, existing.outcome correct discriminator; (5) hmac.js edit additive exports-only (2 names, 0 body change); (6) approvalId deterministic runId:nodeId:attempt, mappings pure, only side-effect = single ledger append. 4 carried Minors ALL triaged DEFER (M1 Postgres reconciliation plan-deferred; M2 JSDoc 1-word polish—method JSDoc authoritative; M3 store-public consistent w/ ApprovalGate.store convention; M4 granted-resume covered by symmetry, poll returns entry.outcome same path both terminals). 2 NEW Minors found, BOTH in resolveOperatorApproval P8-deferred wrapper, no data-loss/security: (N1) readTerminalDecision returns raw task.gateDecision in snapshot branch vs mapOutcome in fallback — add isCollabGateDecision guard when P8 wires runner; (N2) resolveOperatorApproval consumes replay token even when decision===null (malformed-approval edge) — gate consumption on decision!=null in P8. Both = P8-runner cleanup, non-blocking.

GATES: hitl-kit full suite 244/244 ✓. scripts full MPCO suite 29/29 ✓. hitl-kit typecheck 88 errors ALL pre-existing in 2 untouched test files (escalation-policy-deep 81 + notification-channels 7), ZERO in bridge/store files. Root blast-radius typecheck: RUNNING (slow cold-cache ~57m; known @datazup/app-chat-contracts dist-race may need 1 re-run per plan) — NOT yet confirmed green.

## Slice Tasks

- Task 3 (P7 OperatorApprovalBridge — scripts/mpco): COMPLETE (impl be504dfc + fix 3e69f68b, review clean after fix loop — spec ✅, quality Approved 0Crit/0Imp/0open). 7/7 tests green. Original review: spec ✅, 1 IMPORTANT (silent-swallow non-ENOENT snapshot errors in readTerminalDecision — dead empty if) + 3 Minors. Fix 3e69f68b: Important RESOLVED (non-ENOENT now console.warn w/ dir+err before fallback, ENOENT still silent, dead if removed — checked sibling convention, brief mandated keep-fallback-but-visible so warn not throw is correct); Minor1 RESOLVED (mapOutcome test now covers granted/human_rejected aliases + unknown→error sentinel); Minor2 RESOLVED (no-ledger-write test also asserts fs.readdirSync(dir) has zero ledger files — catches runId-derived-path regression); Minor3 (consumeReplayTokenOnce falsy-args no-op) SKIPPED by operator decision — P8 deferred. Re-review: fix is purely additive (logging+assertions), happy-path untouched, no regression — PASS. hmac.js APPROVED additive exports-only edit (approvalReplayKey+approvalReplayEntry, defined :242/:246 but were unexported — operator chose export over duplicate; verified NO function-body change). Two-phase invariant verified airtight: verifySignatureAndScope calls verifyApprovalHmac(approval,secret) 2-arg → ledgerPath defaults null → NO ledger write on verify (file-system asserted); consumeReplayTokenOnce exactly-once (ledger.length===1); readTerminalDecision prefers real persisted snapshot. Daemon interleaved 2 unrelated commits (80cd5bb7, 40c7130a) between MPCO commits — both touch ZERO mpco files (verified). Bonus: resolveOperatorApproval orchestrator present (not required surface, for P8 wiring).
- Task 2 (P6 RuntimeApprovalBridge — dzupagent hitl-kit): complete (commit b5716afa, review clean — spec ✅, quality Approved 0Crit/0Imp). 6/6 tests green (brief's "7" was a miscount — T10a/T10b each one it()). Real store-driven tests (grant/reject/5ms-timeout drive the bridge, no mocks). Error specificity verified: ensurePending catches DuplicateApprovalError SPECIFICALLY + rethrows others; pollTerminal catches ApprovalTimeoutError→'timeout' + rethrows others; mapOutcome granted→human_approved/rejected→human_rejected (not inverted, store literals confirmed). approvalId exact `runId:nodeId:attempt`. TerminalGateDecision = Extract<CollabGateDecision,...> (stays in sync). package.json dependencies block well-formed (adapter-types "\*", no cycle — type root). Typecheck 88 baseline / 0 new in bridge file. Needed `yarn workspace @dzupagent/adapter-types build` to refresh stale dist (benign workspace dist-staleness). MINOR(carry to final): M3 `store` field public (tests rely on it; undercuts abstraction — consider private/protected); M4 granted-resume immediate-return path only covered via async race not direct (T10b covers rejected directly — same mechanism).
- Task 1 (P6a hitl-kit createPending idempotency — dzupagent): complete (commit 041c2565, review clean — spec ✅, quality Approved 0Crit/0Imp). 99/99 tests green (3 new MPCO P6 tests + 3 pre-existing pending-duplicate-throws assertions updated to terminal case across approval-gate.test.ts AND hitl-approval-gate-deep.test.ts — both verified genuinely re-test terminal-throws, not gutted). Logic delta surgical: createPending now `entries.get`→ if existing && !existing.outcome return (idempotent, no payload/waiter clobber), throw only if existing.outcome (terminal). Interface JSDoc :46 tightened. Large diff = formatter reflow (single→double quotes + semicolons + param wrap) — known benign repo pattern, NOT logic. 3 files (not 2): deep-test file authorized by brief. MINOR(carry to final): M1 Postgres-store test asserts pending-duplicate-throws (Postgres impl still throws on ANY dup — deferred divergence, add deviation comment); M2 class-level ApprovalStateStore JSDoc :36 says "pending request...terminal state" slightly ambiguous vs method JSDoc (1-word fix). KNOWN: typecheck has 81 PRE-EXISTING errors in escalation-policy-deep.test.ts (present on base d9ba56dd, file NOT touched by this commit — verified).

---

# SDD Progress: MPCO Foundation (P1 → P1b → P5)

NEXT SLICE PLAN WRITTEN (P6+P7 approval bridges — awaiting operator review, not yet executed): workspace-docs/repos/scripts/docs/mpco-approval-bridges-plan-2026-06-29.md — 3 tasks: T1 hitl-kit createPending idempotency patch (idempotent-on-pending, throw-on-terminal); T2 RuntimeApprovalBridge in @dzupagent/hitl-kit (ensurePending/pollTerminal/mapOutcome→CollabGateDecision, id runId:nodeId:attempt, T10); T3 OperatorApprovalBridge in scripts/mpco (.mjs, two-phase verify→read→consumeOnce, prefers CollabTask snapshot on resume, T11). dzupagent T1+T2 → NEW branch feat/mpco-approval-bridges cut from feat/mpco-resolver-envelope-gate (live tip d9ba56dd); scripts T3 → continue feat/mpco-foundation (live tip 625455ae). Verified drifts corrected: createPending call site :74 not :68; ApprovalAlreadyUsedError at hmac.js:346 not :336; buildApprovalDocument at approval-document.js:56 not :32; branch SHAs were stale. KEY EXTRA HAZARD (spec didn't flag): verifyApprovalHmac APPENDS to the ledger on successful verify — phase-1 verify MUST pass ledgerPath:null to keep two-phase separation. No contract unsatisfiable; 8 new symbol names collision-free. Self-reviewed: SHAs + ledger side-effect spot-checked vs live source, both confirmed.

(prior slice) NEXT SLICE PLAN WRITTEN (awaiting operator review, not yet executed): workspace-docs/repos/scripts/docs/mpco-resolver-envelope-gate-plan-2026-06-29.md — P2 resolver + P3 envelope + P4 command-gate, 7 tasks, source-verified. Risks resolved: run-validation.js bug at :154 (not :149); executor enum reconciliation 2-sided (impl+test, none→advisory floor, add blocks_auto_accept); new contract names collision-free; no mpco:test script (uses node --test). dzupagent type surface → feat/mpco-resolver-envelope-gate; scripts → continue feat/mpco-foundation.

---

# SDD Progress: MPCO Resolver+Envelope+Gate (P2 → P3 → P4) — IN FLIGHT (started 2026-06-29)

Plan: workspace-docs/repos/scripts/docs/mpco-resolver-envelope-gate-plan-2026-06-29.md
dzupagent branch: feat/mpco-resolver-envelope-gate base 20b0fd76 (cut from feat/mpco-foundation)
scripts branch: feat/mpco-foundation base dc41c63f (continues foundation; mpco/ + impl-orch/ clean, 10 unrelated dirty files present — commit by explicit path only)

## Slice Tasks

- Task 1 (P2 resolver — dzupagent adapter-types): complete (commit 6f3d6b73, review clean — spec ✅, quality approved). 7/7 tests pass (brief said "8" — harmless miscount; 7 it() blocks cover all named behaviors), typecheck EXIT 0. ⚠️ resolved: AdapterProviderId includes goose/openrouter (confirmed by typecheck green); barrel additive. MINOR(carry to final): formatter reflowed index.ts quotes/semicolons (cosmetic); ResolutionSource has unused 'nondeterministic' member (plan-mandated, forward-looking — used by later escalation logic).
- Task 2 (P2 scripts projection): complete (scripts commit 2fb44cd3, review clean — spec ✅, quality approved). 3/3 tests pass. Commit scope verified clean (only 2 mpco/ files; 0 unrelated dirty files swept in).
- Task 3 (P3 CollabTask envelope + scripts persistence): complete (dzupagent 33dab0ab 2/2 + scripts c90cb66d 5/5, review clean — spec ✅, quality approved). Evidence boundary enforced BOTH at type level (EvidenceRef.digestOf: "sanitized" literal) AND runtime (assertEvidenceSanitized checks digestOf!=='sanitized' + raw-events uri substring). Atomic write (tmp+renameSync) test-verified no .tmp leftover. resolution:ProviderResolution wires the P2 dep. dzupagent typecheck EXIT 0. DEVIATION (sound): test used `!` non-null assertions on task.parts[0] for noUncheckedIndexedAccess — array statically non-empty, test-local hygiene. Both commit scopes clean (no unrelated dirt). ⚠️ resolved: AdapterProviderId accepts codex/claude (typecheck green). MINOR(carry to final): assertEvidenceSanitized uses `part.evidence ?? []` (defensive, lets zero-evidence rationale pass silently).
- Task 4 (P4 CommandGateResult + aggregation — dzupagent): complete (commit 68284ddc, review clean — spec ✅, quality approved). 5/5 tests pass (brief said "6" — harmless miscount; 5 cover T9a/T9b/empty-floor/T9c/T9d), typecheck EXIT 0. DecisionImpact exactly 3 (no 'none'); IMPACT_RANK {advisory:0,blocks_auto_accept:1,blocks_acceptance:2}; applyAllowFailure spreads new obj, never touches status (T9c asserts both); aggregate empty→advisory. Commit scope clean (3 files, 1-line barrel append).
- Task 5 (P4 scripts command-gate projection): complete (scripts commit 1ac85ae5, review clean — spec ✅, quality approved no findings, reviewer re-ran 4/4 green). Enum strings exactly match dzupagent (advisory/blocks_auto_accept/blocks_acceptance, no 'none'); status passthrough verbatim; allowFailure only downgrades impact. Optional mpco:test package.json add SKIPPED (package.json is dirty). Commit scope clean (2 mpco/ files only).
- Task 6 (P4 fix lossy run-validation.js): complete (scripts commit 63a71cc5, review clean — spec ✅, quality approved). 11/11 tests (8 pre-existing stayed green + 3 new). status derived from exitCode ALONE (allowFailure NOT in status expr — verified); passed retained byte-identical for back-compat; decisionImpact advisory|blocks_acceptance. The diff -2 was formatter reflow of two throw stmts (NO field/test dropped — verified). Commit scope clean (2 impl-orch files).
- Task 7 (P4 reconcile executor worstImpact enum): complete (scripts commit 18cdc0c3, review clean — spec ✅, quality approved 1 Minor). 10/10 tests (P8-prep approval/complete tests 6-10 stayed green). All 5 'none' occurrences reconciled (3 impl + 2 test), zero remain (grep-verified). Rank now {advisory:0,blocks_auto_accept:1,blocks_acceptance:2}, seed/floor "advisory". NO node-handling behavior changed (vocab-only). MINOR(carry to final): test doesn't directly pin rank-table contents — a reintroduced 'none' member wouldn't be caught unless something emits "none"; a direct worstImpact unit test (or a blocks_auto_accept-path assertion) would close the gap. BENIGN deviation: TDD-red didn't fire (old worstImpact("none","advisory") already returned "advisory" since advisory outranked none) — impl still correctly reconciled.
  AUTOCOMMITTER NOTE: a background daemon commit 2d0f7f1f "Stabilize app dev smoke checks" (browser-smoke + start-apps) interleaved between Task 6 (63a71cc5) and Task 7 (18cdc0c3) on scripts feat/mpco-foundation. It touched ZERO MPCO files — all 5 MPCO scripts commits (2fb44cd3, c90cb66d, 1ac85ae5, 63a71cc5, 18cdc0c3) verified surgically scoped to their intended files only. Slice integrity intact; daemon commit is unrelated WIP, harmless to the MPCO work.

## Slice Closeout (P2/P3/P4)

- adapter-types full suite: 40/40 PASS (12 files inc. provider-origin 7, collab-task 2, command-gate 5; all pre-existing unaffected). adapter-types typecheck EXIT 0.
- scripts MPCO + run-validation suite: 33/33 PASS (provider-origin-projection, collab-persistence, command-gate-projection, execute-canonical-ast, run-validation together).
- Root blast-radius typecheck: ran 57m (cold cache). EXIT-with-errors but the 3 failures are PRE-EXISTING and UNRELATED to MPCO — verified: (a) NONE of the 3 failing apps (arabic-language-app/api, scripts-app/api, template-app/api) depend on @dzupagent/adapter-types; (b) ZERO errors reference any MPCO surface (adapter-types/provider-origin/collab-task/command-gate/run-validation/execute-canonical/DecisionImpact/ProviderResolution — grep clean); (c) arabic = real TS errors in authenticate.test.ts (Express NextFunction sig mismatch, broker-auth middleware test, last touched by 4c077674 unrelated); scripts-app+template-app = pure "can't satisfy dependency graph" cascade (the pre-existing dist-race the plan flagged), no errors of their own. @dzupagent/adapter-types (the only changed package) typechecks EXIT 0. MPCO blast radius is CLEAN.

## FINAL WHOLE-BRANCH REVIEW (opus, both repos): MERGE-READY = YES. 0 Critical, 0 Important. All 6 cross-cutting invariants PASS verified vs LIVE on-disk state (DecisionImpact 3-value identical in all 4 sites no 'none'; ModelOriginFamily single source; P2→P3 resolution:ProviderResolution real type dep; evidence boundary both type+runtime; allowFailure never rewrites status in both repos; resolver+aggregators pure). All 4 carried Minors (M1 barrel reflow, M2 unused nondeterministic, M3 evidence ?? [], M4 rank-table not pinned) triaged DEFER. No scope creep, no placeholders/assertion-free tests. DEFERRED HARDENING (cheap, non-blocking): M4 add one-line worstImpact('advisory','blocks_auto_accept') assertion; add a TS↔mjs seam contract/golden test (scripts projections hand-mirror DecisionImpact enum + codex/claude origin-map + CommandGateResult shape — nothing guards future one-sided drift).

## Slice Status: ALL 7 TASKS COMPLETE + REVIEWED CLEAN + FINAL REVIEW MERGE-READY=YES. Awaiting operator merge decision.

- dzupagent feat/mpco-resolver-envelope-gate: 20b0fd76..68284ddc (3 commits: 6f3d6b73 P2, 33dab0ab P3, 68284ddc P4). NOT merged.
- scripts feat/mpco-foundation: 5 new MPCO commits (2fb44cd3 P2, c90cb66d P3, 1ac85ae5 P4-proj, 63a71cc5 P4-fix, 18cdc0c3 P4-reconcile) + 1 unrelated daemon commit 2d0f7f1f interleaved. NOT merged.

Plan: workspace-docs/repos/scripts/docs/mpco-foundation-implementation-plan-2026-06-29.md
dzupagent branch: feat/mpco-foundation base c4dda519
scripts branch: feat/mpco-foundation base d9c2cba6

## Tasks

- Task 1 (P1 RUNTIME*LEAF routing — dzupagent/flow-compiler): complete (commits c4dda519..6176b7bc, review clean — d4fb4159 impl + 6176b7bc fix). Review caught IMPORTANT: RUNTIME_LEAF overcapture onto fleet.*/knowledge._/worker.dispatch (real source grouped them with adapter._ in one case block; brief assumed adapter.\_ standalone) — FIXED by splitting case + T4 regression test. 31/31 pass, pkg typecheck EXIT 0. MINOR(carry to final): test-file quote/semicolon reformat churn.
- Task 2 (P1b canonical-AST executor — scripts/mpco): complete (scripts repo commits 6795e049 impl + 02bec1cd tests, review clean). 5/5 pass. Review caught 2 IMPORTANT test gaps (else-path + unknown-node throw untested) — FIXED with 2 added tests, impl unchanged (blob 99be3f0b). NOTE: scripts feat/mpco-foundation also carries 3 PRE-EXISTING unrelated commits already on main (b04d6122 chore compose, 6f33eb6c fix workspace audit, 0e566a89 test compose) — they sit between session-start base d9c2cba6 and Task2's parent; harmless on merge (already on main). Task2's true review base = 6795e049^ = 0e566a89. MINOR(carry to final): adapter.run step omits confidence; evalCondition ctx=state untested.
- Task 3 (P5 collab.review*loop macro expansion — dzupagent/flow-dsl): complete (dzupagent commit 7f1eef26, review clean on opus). 8/8 expand tests + full flow-dsl 319/319, typecheck+lint EXIT 0. KEY CORRECTION: brief+my ground-truth cited WRONG layer (flow-ast/parse/*); live pipeline parseDslToDocument→normalizeDslDocument→validateDocument runs flow-dsl/src/normalize\_ layer. Implementer pivoted (verified by reviewer against source): conditional key `if` NOT `branch`; approval body `on_approve`; adapter.run REQUIRES non-empty `output`; validate omitted when no gate commands; top-level `steps` NOT `nodes`; mini-yaml is BLOCK-STYLE ONLY (no inline {}/[{}]); T7 source needs dsl/id/version:1. MINOR(carry to final): parse-dsl.ts whole-file prettier reflow inflates diff (~8 real lines); expandOne output aliases input commands/meta (benign, pure-by-no-mutation holds); T5a whitelist looser than emitted.

## Closeout

- Root yarn typecheck: PASS for MPCO blast radius. First run EXIT=1 but the ONLY error was a transient shared-kit dist race: @datazup/app-chat-contracts dist/index.d.ts not yet emitted when @reward-app/api + @scripts-app/api typechecked (TS7016). NO error referenced flow-compiler/flow-dsl/RUNTIME_LEAF/route-target/expand-collab/reason. flow-compiler + flow-dsl typecheck EXIT=0; after dist built, both reward-app/api + scripts-app/api re-typecheck EXIT=0. Pre-existing, unrelated to MPCO (shared-kit on feat/broker-hosted-auth-google-social).
- FINAL whole-branch review (opus, both repos): READY TO MERGE — Yes. 0 Critical, 0 Important. All 4 Global Constraints verified vs source in final state; 3-task seam vocabulary-coherent (if→branch normalization is the correct layer boundary, not a mismatch); both per-task fixes hold (T1 case-split+T4, T2 else/throw tests); no scope creep beyond foundation slice; all 4 carried minors triage ACCEPTABLE. ARCH NOTE (deferred to P8 runner, NOT a defect): P1b executor handles sequence/adapter.run/validate/branch but not approval/complete (which P5 emits) — fails safe via default throw; P5 is compile-only & never feeds P1b in this slice. P8 must extend executor node matrix.
- Merge feat/mpco-foundation (both repos): DEFERRED by operator choice (Keep branches as-is). Branches preserved, NOT merged, NOT pushed. Operator merges per shared-repo protocol.
  MERGE RECIPE (next session):
  - dzupagent (SHARED — protocol-bound): isolated worktree + NODE_AUTH_TOKEN, NEVER live. `git -C dzupagent worktree add ../dz-merge main` → merge feat/mpco-foundation → root `yarn typecheck` (expect the transient app-chat-contracts dist race; build shared-app-kit first or re-run) → merge to dzupagent main. Range c4dda519..7f1eef26 (commits d4fb4159, 6176b7bc, 7f1eef26).
  - scripts (NOT live-linked, lower risk): merge feat/mpco-foundation → main. ONLY mpco/ commits are new (6795e049, 02bec1cd); the other 3 (b04d6122, 6f33eb6c, 0e566a89) are already on main — clean fast-forward/merge.
  - FOLLOW-UP (P8 runner): extend scripts/mpco/execute-canonical-ast.mjs node matrix to approval + complete (P5 emits them; executor currently throws on them — fails safe but must be wired before the two halves connect). ✅ DONE 2026-06-29 (scripts feat/mpco-foundation commits cd171c73 impl + 0b766bd0 minor): executor now handles complete (terminal, sets lastOutput from result) + approval (new injected approveGate(node,ctx)->bool port mirroring evalCondition; recurses onApprove/onReject camelCase). 10/10 tests inc. nested P5-shape (approval→onApprove→[complete]). Review clean (1 minor cosmetic fixed). Seam P5↔P1b now closed for these node types.
